This sample shows how to associate a ScaleLine control with a map.  Element binding is used to bind the ScaleBar and other elements to the map and its contents in XAML.
The scale line is customized to look like a double scale bar.